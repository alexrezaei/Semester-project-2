const getBtn = document.querySelector("#getProduct");
const productId = document.querySelector("#product-id");

getBtn.addEventListener("click", (e) => {
    e.preventDefault();
    console.log("hello");
});
